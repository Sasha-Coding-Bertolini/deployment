name: CI/CD Pipeline

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
        working-directory: ./

      - name: Run tests
        run: |
          pytest
        working-directory: ./

- name: Deploy to VPS
  env:
    SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
  run: |
   ssh -i /Users/valentinamazzucato/.ssh/deploy_key root@ubuntu-s-1vcpu-512mb-10gb-fra1-01 "cd /home/farm && git pull origin master && systemctl restart farm.service"

